<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/img_cover_movie"
        android:layout_width="115dp"
        android:layout_height="150dp"
        app:layout_constraintStart_toStartOf="parent"
        android:scaleType="fitXY"
        app:layout_constraintTop_toTopOf="parent"
        tools:background="@color/teal_200"
        android:contentDescription="@string/menampilkan_poster" />

    <TextView
        android:id="@+id/tv_title_movie"
        android:layout_width="115dp"
        android:layout_height="wrap_content"
        android:paddingStart="5dp"
        android:singleLine="true"
        android:paddingTop="5dp"
        android:paddingEnd="5dp"
        android:textColor="@color/white"
        android:textSize="14sp"
        android:textStyle="bold"
        android:ellipsize="end"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/img_cover_movie"
        tools:text="MOVIE NAME" />

    <TextView
        android:id="@+id/tv_category_movie"
        android:layout_width="115dp"
        android:layout_height="wrap_content"
        android:textColor="@color/cardview_shadow_end_color"
        app:layout_constraintStart_toStartOf="parent"
        android:paddingEnd="5dp"
        android:textSize="12sp"
        android:paddingBottom="5dp"
        android:paddingStart="5dp"
        app:layout_constraintTop_toBottomOf="@+id/tv_title_movie"
        tools:text="Category" />

</androidx.constraintlayout.widget.ConstraintLayout>